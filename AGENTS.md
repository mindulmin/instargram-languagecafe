# Language Cafe cloud publisher

Current new-post strategy (2026-09-25): read `content-queue/CHANNEL-SPLIT-v3.md` first. Instagram promotion and Threads English-language cards that teach Korean expressions are independent channels. The v2 policy is disabled for new releases. The remaining learning-pair instructions in this file govern historical v2 verification and recovery only; do not use them to create a new v2 post or convert a historical receipt to v3. The v3 policy is not yet activated.

Read `cloud/README.md`, `AUTOMATION.md`, `content-queue/AUTOMATION-CONVERSION-PUBLISHER.md`, and `content-queue/threads/THREADS-KOREAN-CONVERSATION-TEMPLATE.md` before work.

The owner authorized migration to `mindulmin/instargram-languagecafe` and, on 2026-09-11, cloud content selection, production, review and distribution through one Instagram lesson, its linked Threads recall companion, and at most one reviewed Story per successfully verified pair. The trusted `cloud/controller.cjs` derives a fresh release from `cloud/control/policy.json`, complete official Instagram history, reconciled receipts and a unique ready Sheet/CSV source. A metadata-only draft seed is not a review approval. Only a valid, current `publish_one_learning_pair` contract authorizes lesson production/publication; all existing final review gates remain mandatory. Story publication is a separate post-pair step in `cloud/growth.cjs`, not permission for the publishing agent to post Stories itself.

Preserve every copy, visual, canonical mascot, official readback, and exact-once gate. Never clear an unresolved lock, change the controller to make a post due, fabricate a review, retry ambiguous publishing, or create a Reel. Do not change application, payment, database, profile, or pricing settings.

Cloud execution uses a fresh Windows runner and reproduces the contract's canonical paths. `cloud/control/current-test.json` is the strategy template and historical baseline, not today's release. The trusted controller writes the daily canonical control before a durable hash-bound claim. Its exact bytes, policy and source seed are encrypted in `cloud-state`. The local sibling application is not a live source for cloud runs. Do not replace encrypted state with an empty queue or treat missing state as a first run. Preview mode must never claim, create media, deploy or publish.

Assess accuracy, completeness, usability, and contribution to sustainable operations before accepting changes. Keep source changes narrow and verify observable results.
