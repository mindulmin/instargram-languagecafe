# Language Cafe cloud publisher

Read `cloud/README.md`, `AUTOMATION.md`, `content-queue/AUTOMATION-CONVERSION-PUBLISHER.md`, and `content-queue/threads/THREADS-KOREAN-CONVERSATION-TEMPLATE.md` before work.

The owner authorized migration to `mindulmin/instargram-languagecafe` and GitHub-hosted scheduled execution. This migration authorization permits repository/workflow setup; it does not manufacture a content release. Only a valid, current `publish_one_learning_pair` contract authorizes a social post.

Preserve every copy, visual, canonical mascot, official readback, and exact-once gate. Never clear an unresolved lock, change the controller to make a post due, fabricate a review, retry ambiguous publishing, or create a Reel. Do not change application, payment, database, profile, or pricing settings.

Cloud execution uses a fresh Windows runner and reproduces the contract's canonical paths. The reviewed `cloud/control/current-test.json` is the cloud release authority. The local sibling application is no longer a live source for cloud runs. The encrypted `cloud-state` branch stores mutable publisher state. Do not replace it with an empty queue or treat missing state as a first run.

Assess accuracy, completeness, usability, and contribution to sustainable operations before accepting changes. Keep source changes narrow and verify observable results.
