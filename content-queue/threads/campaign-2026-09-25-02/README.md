# Three-card Korean takeaway lesson — recovery candidate

The 2026-09-25 first Threads API attempt stopped during child-container creation, before a carousel parent or publish intent. This is a **new**, shorter, three-card job; it is not a retry of the old API request or evidence that the first attempt published. Until independent editorial review, a matching grant, and official incident closure, it remains a draft and Threads stays paused.

Cards 1, 3 and 6 from the original unpublished artwork were re-rendered in this order with new 01/03–03/03 numbering. The sequence teaches the expression, shows a real exchange, then asks for a three-minute recall and offers a separate free Korean mission. The text includes exactly one direct mission URL and does not promise the mission teaches this exact line. The renderer makes only local draft files; the three JPEGs were separately hosted at an immutable Cloudflare Pages deployment.

Source: `render-three-card.cjs`; local review sheet: `rendered/phone-preview.png`; hosted asset checks: `asset-hosting-evidence.json`. A 270-pixel-wide preview is a legibility check, not proof of a public Threads carousel.
